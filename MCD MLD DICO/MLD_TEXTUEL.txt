UTILISATEUR_ = (ID_USER VARCHAR(50), LASTNAME_USER VARCHAR(50), FIRSTNAME_USER VARCHAR(50), AGE_USER DATE, EMAIL_USER_ VARCHAR(150), MDP_USER VARCHAR(50), WEIGHT_USER_ DECIMAL(3,3), SIZE_USER DECIMAL(3,3), OBJECT_USER_ VARCHAR(250));
SALLE_DE_SPORT = (ID_SALLE VARCHAR(50), NAME_SALLE VARCHAR(150), ADR_SALLE VARCHAR(250), CITY_SALLE VARCHAR(50), GPS_SALLE DECIMAL(15,15), OPENTIME_SALLE_ DATETIME, AVAILABLE_SERVICE VARCHAR(500), PRICES DECIMAL(4,2), ZIPCODE_SALLE INT);
ENTRAINEMENTS_PERSONNALISE = (ID_PERSONNAL_TRANING VARCHAR(50), TRANING_NAME_ VARCHAR(50), TRANING_DESCRPTION VARCHAR(250), TOT_DURATION_TRANING TIME, DIFF_LEVEL VARCHAR(50), TRANING_CAT VARCHAR(50), EXERCISES_LIST VARCHAR(250));
CATALOGUE_D_ENTRAINEMENTS = (ID_CATALOG_TRANING VARCHAR(50), TRANING_NAME_ VARCHAR(50), TRANING_DESCRPTION VARCHAR(250), TOT_DURATION_TRANING TIME, DIFF_LEVEL VARCHAR(50), TRANING_CAT VARCHAR(50), EXERCISES_LIST VARCHAR(250), PREVIEW VARCHAR(50));
SUIVI = (_ID_FOLLOW_UP VARCHAR(50), FOLLOW_UP_DATE DATE, PROGRESSIONS INT, NB_STEPS INT, TRANING_DONE VARCHAR(50), CALORIES_BURNED DECIMAL(3,3), #ID_USER);
AVIS_ET_EVALUATIONS = (ID_NOTICE VARCHAR(50), COMMENT VARCHAR(250), NOTICE_DATE DATE, #ID_CATALOG_TRANING, #ID_USER, #ID_PERSONNAL_TRANING);
UTILISATEUR_LOCALISE_LA_SALLE_ = (#ID_USER, #ID_SALLE);
UTILISATEUR_DEFINIS = (#ID_USER, #ID_PERSONNAL_TRANING);
